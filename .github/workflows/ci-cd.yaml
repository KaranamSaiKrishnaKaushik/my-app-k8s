name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: dokkaushik
        password: Iamthebest
    - name: Debug - Show Files
      run: |
        pwd
        ls -R

    - name: Build and Push Frontend
      run: |
        docker build -t dokkaushik/my-angular-app ./my-angular-app
        docker push dokkaushik/my-angular-app

    - name: Build and Push Backend
      run: |
        docker build -t dokkaushik/mock ./Mock
        docker push dokkaushik/mock        
    
    - name: Update image tag in web-deployment.yaml
      run: |
          sed -i "s|image: dokkaushik/my-angular-app:.*|image: dokkaushik/my-angular-app:${{ github.sha }}|" k8s/web-deployment.yaml
    
    - name: Update image tag in api-deployment.yaml
      run: |
          sed -i "s|image: dokkaushik/mock:.*|image: dokkaushik/mock:${{ github.sha }}|" k8s/api-deployment.yaml
    